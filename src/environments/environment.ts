// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyAIi_2nBz_Iqa-MhAKE4-wAEjoy76_6nPw',
    authDomain: 'idolosapp.firebaseapp.com',
    databaseURL: 'https://idolosapp.firebaseio.com',
    projectId: 'idolosapp',
    storageBucket: 'idolosapp.appspot.com',
    messagingSenderId: '136124610831',
    appId: '1:136124610831:web:2c71f655023da9480ba67a',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
